package execution_service

import (
	"testing"
)

func TestSum(t *testing.T) {
	//cmd := newExecutor("/usr/bin/openssl aes-256-cbc -e -pbkdf2 -pass file:#{password-file} -in #{source} -out #{target}")
	//
	//
	//cmd.Exec(map[string]string{
	//	`password-file`: `password.txt`,
	//	`source`: `source file.txt`,
	//	`target`: `target file.enc`,
	//})

	//if total != 10 {
	//	t.Errorf("Sum was incorrect, got: %d, want: %d.", total, 10)
	//}
}
